name: smsml-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "adult_preprocessing/processed.csv"}
    command: "python modelling.py --data_path {data_path}"